// update the likes count
$('#likes_cnt_<%= params[:id] %>').html("<%= @photo.likes.count %>");
// update the button
var elem = $('#like_<%= params[:id] %>');
elem.replaceWith("<%= escape_javascript(button_tag("", :type => "submit",:id => 'unlike_' + @photo.id.to_s, :value => 'unlike', :name => 'modal', :class => 'me_likey')) %>");
